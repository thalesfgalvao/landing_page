import { styled } from "@mui/material/styles";

export const NotFoundInfo = styled('p')({
    color: 'var(--text-secondary)',
    alignSelf: 'center',
    fontSize: 'var(--textSize-medium);',
})

export const NotFoundLabel = styled('p')({
    color: 'var(--text-secondary)',
    alignSelf: 'center',
    fontSize: '1.2rem',
    fontStyle: 'italic',
})